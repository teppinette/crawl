# Pinned to current crawldevvm venv (verified 2026-06-26)
# Lean set for Container App — gateway dispatches via HTTP, no browser /
# SSH / Multilogin xcli on the container side (those live on crawl-verify).
fastapi==0.135.3
uvicorn[standard]==0.44.0
httpx==0.28.1
requests==2.33.1
curl-cffi==0.7.4
pydantic==2.13.0
psycopg2-binary==2.9.11
PyYAML==6.0.3
beautifulsoup4==4.14.3
paramiko==4.0.0
# Playwright Python only — no browser binaries. The gateway never calls
# sync_playwright; the import-graph just demands the package be installed.
playwright==1.59.0
# Azure SDK
azure-ai-agents==1.1.0
azure-ai-projects==2.2.0
azure-identity==1.25.3
azure-keyvault-secrets==4.11.0
azure-storage-blob==12.28.0
# python-multipart used by fastapi for form-data POSTs (bulk verify etc.)
python-multipart==0.0.20
